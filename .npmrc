# npm registry mirror
registry=https://registry.npmmirror.com/

# Hoist all dependencies to node_modules/ root so that
# `node dist/index.js` (esbuild --packages=external) can resolve them
shamefully-hoist=true
