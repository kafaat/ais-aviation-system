-- Generated by scripts/ci/forensic-data-audit.ts --print-sql. Read only.
-- Identifiers only; hashed provider references. No automatic reconciliation.
SET TRANSACTION ISOLATION LEVEL REPEATABLE READ;
START TRANSACTION READ ONLY;
-- split_refund_under_review (error; owner: split-refund)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'split_refund_under_review' AS finding, COUNT(*) AS total FROM (SELECT bookingId AS recordId FROM booking_refund_plans WHERE status = 'review_required') findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'split_refund_under_review' AS finding, recordId FROM (SELECT bookingId AS recordId FROM booking_refund_plans WHERE status = 'review_required') findings ORDER BY recordId LIMIT 50;
-- split_refund_allocation_mismatch (error; owner: split-refund)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'split_refund_allocation_mismatch' AS finding, COUNT(*) AS total FROM (SELECT p.bookingId AS recordId FROM booking_refund_plans p LEFT JOIN bookings b ON b.id = p.bookingId LEFT JOIN (SELECT bookingId, COUNT(*) AS itemCount, SUM(collectedAmount) AS collectedTotal, SUM(refundAmount) AS refundTotal, COUNT(DISTINCT planId) AS planCount, MIN(planId) AS planId, SUM(refundAmount <= 0 OR refundAmount > collectedAmount) AS invalidAmounts, SUM(status <> 'succeeded') AS unfinishedItems FROM booking_refund_items GROUP BY bookingId) i ON i.bookingId = p.bookingId WHERE COALESCE(i.itemCount, 0) < 2 OR i.collectedTotal <> p.totalAmount OR i.refundTotal <> p.refundAmount OR p.refundAmount + p.cancellationFee <> p.totalAmount OR i.planCount <> 1 OR i.planId <> p.id OR i.invalidAmounts > 0 OR b.status <> 'cancelled' OR b.status IS NULL OR (p.status = 'completed' AND i.unfinishedItems > 0)) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'split_refund_allocation_mismatch' AS finding, recordId FROM (SELECT p.bookingId AS recordId FROM booking_refund_plans p LEFT JOIN bookings b ON b.id = p.bookingId LEFT JOIN (SELECT bookingId, COUNT(*) AS itemCount, SUM(collectedAmount) AS collectedTotal, SUM(refundAmount) AS refundTotal, COUNT(DISTINCT planId) AS planCount, MIN(planId) AS planId, SUM(refundAmount <= 0 OR refundAmount > collectedAmount) AS invalidAmounts, SUM(status <> 'succeeded') AS unfinishedItems FROM booking_refund_items GROUP BY bookingId) i ON i.bookingId = p.bookingId WHERE COALESCE(i.itemCount, 0) < 2 OR i.collectedTotal <> p.totalAmount OR i.refundTotal <> p.refundAmount OR p.refundAmount + p.cancellationFee <> p.totalAmount OR i.planCount <> 1 OR i.planId <> p.id OR i.invalidAmounts > 0 OR b.status <> 'cancelled' OR b.status IS NULL OR (p.status = 'completed' AND i.unfinishedItems > 0)) findings ORDER BY recordId LIMIT 50;
-- split_refund_worker_overdue (review; owner: split-refund)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'split_refund_worker_overdue' AS finding, COUNT(*) AS total FROM (SELECT DISTINCT bookingId AS recordId FROM booking_refund_items WHERE status IN ('queued','requesting','pending') AND nextAttemptAt < DATE_SUB(NOW(), INTERVAL 15 MINUTE)) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'split_refund_worker_overdue' AS finding, recordId FROM (SELECT DISTINCT bookingId AS recordId FROM booking_refund_items WHERE status IN ('queued','requesting','pending') AND nextAttemptAt < DATE_SUB(NOW(), INTERVAL 15 MINUTE)) findings ORDER BY recordId LIMIT 50;
-- booking_missing_owner (error; owner: bookings)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'booking_missing_owner' AS finding, COUNT(*) AS total FROM (SELECT b.id AS recordId FROM bookings b LEFT JOIN users u ON u.id = b.userId WHERE b.userId <= 0 OR u.id IS NULL) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'booking_missing_owner' AS finding, recordId FROM (SELECT b.id AS recordId FROM bookings b LEFT JOIN users u ON u.id = b.userId WHERE b.userId <= 0 OR u.id IS NULL) findings ORDER BY recordId LIMIT 50;
-- legacy_tenant_unassigned (review; owner: tenant)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'legacy_tenant_unassigned' AS finding, COUNT(*) AS total FROM (SELECT id AS recordId FROM bookings WHERE tenantId IS NULL) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'legacy_tenant_unassigned' AS finding, recordId FROM (SELECT id AS recordId FROM bookings WHERE tenantId IS NULL) findings ORDER BY recordId LIMIT 50;
-- agency_without_owner (error; owner: travel-agent)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'agency_without_owner' AS finding, COUNT(*) AS total FROM (SELECT a.id AS recordId FROM travel_agents a LEFT JOIN users u ON u.id = a.ownerUserId WHERE a.ownerUserId IS NULL OR u.id IS NULL) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'agency_without_owner' AS finding, recordId FROM (SELECT a.id AS recordId FROM travel_agents a LEFT JOIN users u ON u.id = a.ownerUserId WHERE a.ownerUserId IS NULL OR u.id IS NULL) findings ORDER BY recordId LIMIT 50;
-- passenger_count_mismatch (error; owner: bookings)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'passenger_count_mismatch' AS finding, COUNT(*) AS total FROM (SELECT b.id AS recordId FROM bookings b LEFT JOIN passengers p ON p.bookingId = b.id GROUP BY b.id, b.numberOfPassengers HAVING COUNT(p.id) <> b.numberOfPassengers) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'passenger_count_mismatch' AS finding, recordId FROM (SELECT b.id AS recordId FROM bookings b LEFT JOIN passengers p ON p.bookingId = b.id GROUP BY b.id, b.numberOfPassengers HAVING COUNT(p.id) <> b.numberOfPassengers) findings ORDER BY recordId LIMIT 50;
-- orphan_passenger (error; owner: bookings)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'orphan_passenger' AS finding, COUNT(*) AS total FROM (SELECT p.id AS recordId FROM passengers p LEFT JOIN bookings b ON b.id = p.bookingId WHERE b.id IS NULL) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'orphan_passenger' AS finding, recordId FROM (SELECT p.id AS recordId FROM passengers p LEFT JOIN bookings b ON b.id = p.bookingId WHERE b.id IS NULL) findings ORDER BY recordId LIMIT 50;
-- segment_reservation_mismatch (error; owner: booking-settlement)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'segment_reservation_mismatch' AS finding, COUNT(*) AS total FROM (SELECT b.id AS recordId FROM bookings b JOIN booking_segments s ON s.bookingId = b.id WHERE b.paymentStatus = 'paid' AND b.status = 'confirmed' GROUP BY b.id, b.seatsReserved HAVING SUM(s.seatsReserved) <> COUNT(s.id) OR b.seatsReserved = 0) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'segment_reservation_mismatch' AS finding, recordId FROM (SELECT b.id AS recordId FROM bookings b JOIN booking_segments s ON s.bookingId = b.id WHERE b.paymentStatus = 'paid' AND b.status = 'confirmed' GROUP BY b.id, b.seatsReserved HAVING SUM(s.seatsReserved) <> COUNT(s.id) OR b.seatsReserved = 0) findings ORDER BY recordId LIMIT 50;
-- segment_allocation_mismatch (error; owner: booking-settlement)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'segment_allocation_mismatch' AS finding, COUNT(*) AS total FROM (SELECT b.id AS recordId FROM bookings b JOIN booking_segments s ON s.bookingId = b.id GROUP BY b.id, b.totalAmount HAVING COUNT(s.segmentAmount) <> COUNT(s.id) OR SUM(s.segmentAmount) <> b.totalAmount) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'segment_allocation_mismatch' AS finding, recordId FROM (SELECT b.id AS recordId FROM bookings b JOIN booking_segments s ON s.bookingId = b.id GROUP BY b.id, b.totalAmount HAVING COUNT(s.segmentAmount) <> COUNT(s.id) OR SUM(s.segmentAmount) <> b.totalAmount) findings ORDER BY recordId LIMIT 50;
-- segment_identity_mismatch (error; owner: multi-city)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'segment_identity_mismatch' AS finding, COUNT(*) AS total FROM (SELECT s.id AS recordId FROM booking_segments s LEFT JOIN bookings b ON b.id = s.bookingId LEFT JOIN flights f ON f.id = s.flightId WHERE b.id IS NULL OR f.id IS NULL OR NOT (b.tenantId <=> f.tenantId)) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'segment_identity_mismatch' AS finding, recordId FROM (SELECT s.id AS recordId FROM booking_segments s LEFT JOIN bookings b ON b.id = s.bookingId LEFT JOIN flights f ON f.id = s.flightId WHERE b.id IS NULL OR f.id IS NULL OR NOT (b.tenantId <=> f.tenantId)) findings ORDER BY recordId LIMIT 50;
-- invoice_ancillary_inconsistency (error; owner: bookings)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'invoice_ancillary_inconsistency' AS finding, COUNT(*) AS total FROM (SELECT b.id AS recordId FROM bookings b JOIN booking_ancillaries a ON a.bookingId = b.id AND a.status = 'active' GROUP BY b.id, b.totalAmount HAVING SUM(a.totalPrice) > b.totalAmount OR SUM(a.quantity <= 0 OR a.unitPrice < 0 OR a.totalPrice <> a.quantity * a.unitPrice) > 0) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'invoice_ancillary_inconsistency' AS finding, recordId FROM (SELECT b.id AS recordId FROM bookings b JOIN booking_ancillaries a ON a.bookingId = b.id AND a.status = 'active' GROUP BY b.id, b.totalAmount HAVING SUM(a.totalPrice) > b.totalAmount OR SUM(a.quantity <= 0 OR a.unitPrice < 0 OR a.totalPrice <> a.quantity * a.unitPrice) > 0) findings ORDER BY recordId LIMIT 50;
-- booking_flight_tenant_mismatch (error; owner: tenant)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'booking_flight_tenant_mismatch' AS finding, COUNT(*) AS total FROM (SELECT b.id AS recordId FROM bookings b LEFT JOIN flights f ON f.id = b.flightId WHERE f.id IS NULL OR NOT (b.tenantId <=> f.tenantId)) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'booking_flight_tenant_mismatch' AS finding, recordId FROM (SELECT b.id AS recordId FROM bookings b LEFT JOIN flights f ON f.id = b.flightId WHERE f.id IS NULL OR NOT (b.tenantId <=> f.tenantId)) findings ORDER BY recordId LIMIT 50;
-- passenger_tenant_mismatch (error; owner: tenant)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'passenger_tenant_mismatch' AS finding, COUNT(*) AS total FROM (SELECT p.id AS recordId FROM passengers p JOIN bookings b ON b.id = p.bookingId WHERE NOT (p.tenantId <=> b.tenantId)) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'passenger_tenant_mismatch' AS finding, recordId FROM (SELECT p.id AS recordId FROM passengers p JOIN bookings b ON b.id = p.bookingId WHERE NOT (p.tenantId <=> b.tenantId)) findings ORDER BY recordId LIMIT 50;
-- unlinked_legacy_hold (review; owner: inventory-capacity)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'unlinked_legacy_hold' AS finding, COUNT(*) AS total FROM (SELECT id AS recordId FROM seat_holds WHERE inventoryLockId IS NULL AND status = 'active') findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'unlinked_legacy_hold' AS finding, recordId FROM (SELECT id AS recordId FROM seat_holds WHERE inventoryLockId IS NULL AND status = 'active') findings ORDER BY recordId LIMIT 50;
-- collected_funds_under_review (error; owner: payment-settlement)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'collected_funds_under_review' AS finding, COUNT(*) AS total FROM (SELECT SHA2(paymentIntentId, 256) AS recordId FROM payment_receipts WHERE settlementStatus = 'review_required') findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'collected_funds_under_review' AS finding, recordId FROM (SELECT SHA2(paymentIntentId, 256) AS recordId FROM payment_receipts WHERE settlementStatus = 'review_required') findings ORDER BY recordId LIMIT 50;
-- terminal_booking_retains_resources (error; owner: booking-settlement)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'terminal_booking_retains_resources' AS finding, COUNT(*) AS total FROM (SELECT b.id AS recordId FROM bookings b WHERE b.status = 'cancelled' AND (b.seatsReserved = 1 OR EXISTS (SELECT 1 FROM booking_segments s WHERE s.bookingId = b.id AND (s.seatsReserved = 1 OR s.status <> 'cancelled')) OR EXISTS (SELECT 1 FROM seat_inventory si WHERE si.bookingId = b.id))) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'terminal_booking_retains_resources' AS finding, recordId FROM (SELECT b.id AS recordId FROM bookings b WHERE b.status = 'cancelled' AND (b.seatsReserved = 1 OR EXISTS (SELECT 1 FROM booking_segments s WHERE s.bookingId = b.id AND (s.seatsReserved = 1 OR s.status <> 'cancelled')) OR EXISTS (SELECT 1 FROM seat_inventory si WHERE si.bookingId = b.id))) findings ORDER BY recordId LIMIT 50;
-- ndc_booking_status_mismatch (error; owner: ndc)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'ndc_booking_status_mismatch' AS finding, COUNT(*) AS total FROM (SELECT n.id AS recordId FROM ndc_orders n LEFT JOIN bookings b ON b.id = n.bookingId WHERE b.id IS NULL OR n.totalAmount <> b.totalAmount OR (b.paymentStatus = 'refunded' AND n.status <> 'refunded') OR (b.status = 'cancelled' AND n.status NOT IN ('cancelled','refunded')) OR (b.status = 'confirmed' AND b.paymentStatus = 'paid' AND n.status = 'pending')) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'ndc_booking_status_mismatch' AS finding, recordId FROM (SELECT n.id AS recordId FROM ndc_orders n LEFT JOIN bookings b ON b.id = n.bookingId WHERE b.id IS NULL OR n.totalAmount <> b.totalAmount OR (b.paymentStatus = 'refunded' AND n.status <> 'refunded') OR (b.status = 'cancelled' AND n.status NOT IN ('cancelled','refunded')) OR (b.status = 'confirmed' AND b.paymentStatus = 'paid' AND n.status = 'pending')) findings ORDER BY recordId LIMIT 50;
-- loyalty_credit_adoption_pending (review; owner: loyalty-balance)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_credit_adoption_pending' AS finding, COUNT(*) AS total FROM (SELECT id AS recordId FROM loyalty_accounts WHERE creditLotsInitializedAt IS NULL) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_credit_adoption_pending' AS finding, recordId FROM (SELECT id AS recordId FROM loyalty_accounts WHERE creditLotsInitializedAt IS NULL) findings ORDER BY recordId LIMIT 50;
-- loyalty_account_missing_owner (error; owner: loyalty-balance)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_account_missing_owner' AS finding, COUNT(*) AS total FROM (SELECT a.id AS recordId FROM loyalty_accounts a LEFT JOIN users u ON u.id = a.userId WHERE u.id IS NULL) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_account_missing_owner' AS finding, recordId FROM (SELECT a.id AS recordId FROM loyalty_accounts a LEFT JOIN users u ON u.id = a.userId WHERE u.id IS NULL) findings ORDER BY recordId LIMIT 50;
-- loyalty_ledger_owner_mismatch (error; owner: loyalty-balance)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_ledger_owner_mismatch' AS finding, COUNT(*) AS total FROM (SELECT t.id AS recordId FROM miles_transactions t LEFT JOIN loyalty_accounts a ON a.id = t.loyaltyAccountId WHERE a.id IS NULL OR t.userId <> a.userId) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_ledger_owner_mismatch' AS finding, recordId FROM (SELECT t.id AS recordId FROM miles_transactions t LEFT JOIN loyalty_accounts a ON a.id = t.loyaltyAccountId WHERE a.id IS NULL OR t.userId <> a.userId) findings ORDER BY recordId LIMIT 50;
-- loyalty_ledger_transition_mismatch (error; owner: loyalty-balance)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_ledger_transition_mismatch' AS finding, COUNT(*) AS total FROM (SELECT id AS recordId FROM (SELECT id, balanceAfter, SUM(amount) OVER (PARTITION BY loyaltyAccountId ORDER BY id ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS recordedBalance FROM miles_transactions) ledger WHERE balanceAfter <> recordedBalance) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_ledger_transition_mismatch' AS finding, recordId FROM (SELECT id AS recordId FROM (SELECT id, balanceAfter, SUM(amount) OVER (PARTITION BY loyaltyAccountId ORDER BY id ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS recordedBalance FROM miles_transactions) ledger WHERE balanceAfter <> recordedBalance) findings ORDER BY recordId LIMIT 50;
-- loyalty_ledger_balance_mismatch (error; owner: loyalty-balance)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_ledger_balance_mismatch' AS finding, COUNT(*) AS total FROM (SELECT a.id AS recordId FROM loyalty_accounts a LEFT JOIN miles_transactions t ON t.loyaltyAccountId = a.id GROUP BY a.id, a.currentMilesBalance, a.totalMilesEarned, a.milesRedeemed HAVING a.currentMilesBalance <> COALESCE(SUM(t.amount), 0) OR (COUNT(t.id) = 0 AND (a.totalMilesEarned <> 0 OR a.milesRedeemed <> 0))) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_ledger_balance_mismatch' AS finding, recordId FROM (SELECT a.id AS recordId FROM loyalty_accounts a LEFT JOIN miles_transactions t ON t.loyaltyAccountId = a.id GROUP BY a.id, a.currentMilesBalance, a.totalMilesEarned, a.milesRedeemed HAVING a.currentMilesBalance <> COALESCE(SUM(t.amount), 0) OR (COUNT(t.id) = 0 AND (a.totalMilesEarned <> 0 OR a.milesRedeemed <> 0))) findings ORDER BY recordId LIMIT 50;
-- loyalty_credit_lot_conservation_mismatch (error; owner: loyalty-balance)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_credit_lot_conservation_mismatch' AS finding, COUNT(*) AS total FROM (SELECT transactionId AS recordId FROM loyalty_credit_lots WHERE creditedMiles <= 0 OR remainingMiles < 0 OR spentMiles < 0 OR expiredMiles < 0 OR reversedMiles < 0 OR creditedMiles <> remainingMiles + spentMiles + expiredMiles + reversedMiles) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_credit_lot_conservation_mismatch' AS finding, recordId FROM (SELECT transactionId AS recordId FROM loyalty_credit_lots WHERE creditedMiles <= 0 OR remainingMiles < 0 OR spentMiles < 0 OR expiredMiles < 0 OR reversedMiles < 0 OR creditedMiles <> remainingMiles + spentMiles + expiredMiles + reversedMiles) findings ORDER BY recordId LIMIT 50;
-- loyalty_credit_lot_identity_mismatch (error; owner: loyalty-balance)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_credit_lot_identity_mismatch' AS finding, COUNT(*) AS total FROM (SELECT l.transactionId AS recordId FROM loyalty_credit_lots l LEFT JOIN miles_transactions t ON t.id = l.transactionId LEFT JOIN loyalty_accounts a ON a.id = l.loyaltyAccountId WHERE a.id IS NULL OR t.id IS NULL OR t.loyaltyAccountId <> l.loyaltyAccountId OR t.userId <> a.userId OR t.type NOT IN ('earn','bonus','adjustment') OR t.amount <> l.creditedMiles OR NOT (t.bookingId <=> l.bookingId) OR NOT (t.expiresAt <=> l.expiresAt)) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_credit_lot_identity_mismatch' AS finding, recordId FROM (SELECT l.transactionId AS recordId FROM loyalty_credit_lots l LEFT JOIN miles_transactions t ON t.id = l.transactionId LEFT JOIN loyalty_accounts a ON a.id = l.loyaltyAccountId WHERE a.id IS NULL OR t.id IS NULL OR t.loyaltyAccountId <> l.loyaltyAccountId OR t.userId <> a.userId OR t.type NOT IN ('earn','bonus','adjustment') OR t.amount <> l.creditedMiles OR NOT (t.bookingId <=> l.bookingId) OR NOT (t.expiresAt <=> l.expiresAt)) findings ORDER BY recordId LIMIT 50;
-- loyalty_initialized_credit_missing_lot (error; owner: loyalty-balance)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_initialized_credit_missing_lot' AS finding, COUNT(*) AS total FROM (SELECT t.id AS recordId FROM miles_transactions t JOIN loyalty_accounts a ON a.id = t.loyaltyAccountId LEFT JOIN loyalty_credit_lots l ON l.transactionId = t.id WHERE a.creditLotsInitializedAt IS NOT NULL AND t.amount > 0 AND t.type IN ('earn','bonus','adjustment') AND l.transactionId IS NULL) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_initialized_credit_missing_lot' AS finding, recordId FROM (SELECT t.id AS recordId FROM miles_transactions t JOIN loyalty_accounts a ON a.id = t.loyaltyAccountId LEFT JOIN loyalty_credit_lots l ON l.transactionId = t.id WHERE a.creditLotsInitializedAt IS NOT NULL AND t.amount > 0 AND t.type IN ('earn','bonus','adjustment') AND l.transactionId IS NULL) findings ORDER BY recordId LIMIT 50;
-- loyalty_available_credit_mismatch (error; owner: loyalty-balance)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_available_credit_mismatch' AS finding, COUNT(*) AS total FROM (SELECT a.id AS recordId FROM loyalty_accounts a LEFT JOIN loyalty_credit_lots l ON l.loyaltyAccountId = a.id WHERE a.creditLotsInitializedAt IS NOT NULL GROUP BY a.id, a.currentMilesBalance HAVING COALESCE(SUM(l.remainingMiles), 0) <> GREATEST(0, a.currentMilesBalance)) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_available_credit_mismatch' AS finding, recordId FROM (SELECT a.id AS recordId FROM loyalty_accounts a LEFT JOIN loyalty_credit_lots l ON l.loyaltyAccountId = a.id WHERE a.creditLotsInitializedAt IS NOT NULL GROUP BY a.id, a.currentMilesBalance HAVING COALESCE(SUM(l.remainingMiles), 0) <> GREATEST(0, a.currentMilesBalance)) findings ORDER BY recordId LIMIT 50;
-- loyalty_uninitialized_account_has_lots (error; owner: loyalty-balance)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_uninitialized_account_has_lots' AS finding, COUNT(*) AS total FROM (SELECT a.id AS recordId FROM loyalty_accounts a WHERE a.creditLotsInitializedAt IS NULL AND EXISTS (SELECT 1 FROM loyalty_credit_lots l WHERE l.loyaltyAccountId = a.id)) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'loyalty_uninitialized_account_has_lots' AS finding, recordId FROM (SELECT a.id AS recordId FROM loyalty_accounts a WHERE a.creditLotsInitializedAt IS NULL AND EXISTS (SELECT 1 FROM loyalty_credit_lots l WHERE l.loyaltyAccountId = a.id)) findings ORDER BY recordId LIMIT 50;
-- family_pool_balance_mismatch (error; owner: family-pool)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'family_pool_balance_mismatch' AS finding, COUNT(*) AS total FROM (SELECT g.id AS recordId FROM family_groups g LEFT JOIN family_group_members m ON m.groupId = g.id GROUP BY g.id, g.pooledMiles HAVING g.pooledMiles < 0 OR g.pooledMiles <> COALESCE(SUM(m.milesContributed - m.milesRedeemed), 0) OR SUM(m.milesContributed < 0 OR m.milesRedeemed < 0) > 0) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'family_pool_balance_mismatch' AS finding, recordId FROM (SELECT g.id AS recordId FROM family_groups g LEFT JOIN family_group_members m ON m.groupId = g.id GROUP BY g.id, g.pooledMiles HAVING g.pooledMiles < 0 OR g.pooledMiles <> COALESCE(SUM(m.milesContributed - m.milesRedeemed), 0) OR SUM(m.milesContributed < 0 OR m.milesRedeemed < 0) > 0) findings ORDER BY recordId LIMIT 50;
-- family_contribution_ledger_mismatch (error; owner: family-pool)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'family_contribution_ledger_mismatch' AS finding, COUNT(*) AS total FROM (SELECT DISTINCT m.groupId AS recordId FROM (SELECT groupId, userId, SUM(milesContributed) AS contributed FROM family_group_members GROUP BY groupId, userId) m LEFT JOIN (SELECT reason, userId, -SUM(amount) AS contributed FROM miles_transactions WHERE type = 'adjustment' AND amount < 0 AND bookingId IS NULL GROUP BY reason, userId) t ON t.reason = CONCAT('family-pool:', m.groupId) AND t.userId = m.userId WHERE m.contributed <> COALESCE(t.contributed, 0)) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'family_contribution_ledger_mismatch' AS finding, recordId FROM (SELECT DISTINCT m.groupId AS recordId FROM (SELECT groupId, userId, SUM(milesContributed) AS contributed FROM family_group_members GROUP BY groupId, userId) m LEFT JOIN (SELECT reason, userId, -SUM(amount) AS contributed FROM miles_transactions WHERE type = 'adjustment' AND amount < 0 AND bookingId IS NULL GROUP BY reason, userId) t ON t.reason = CONCAT('family-pool:', m.groupId) AND t.userId = m.userId WHERE m.contributed <> COALESCE(t.contributed, 0)) findings ORDER BY recordId LIMIT 50;
-- family_transfer_missing_membership (error; owner: family-pool)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'family_transfer_missing_membership' AS finding, COUNT(*) AS total FROM (SELECT t.id AS recordId FROM miles_transactions t LEFT JOIN family_groups g ON t.reason = CONCAT('family-pool:', g.id) WHERE t.reason LIKE 'family-pool:%' AND (g.id IS NULL OR t.type <> 'adjustment' OR t.amount >= 0 OR t.bookingId IS NOT NULL OR NOT EXISTS (SELECT 1 FROM family_group_members m WHERE m.groupId = g.id AND m.userId = t.userId))) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'family_transfer_missing_membership' AS finding, recordId FROM (SELECT t.id AS recordId FROM miles_transactions t LEFT JOIN family_groups g ON t.reason = CONCAT('family-pool:', g.id) WHERE t.reason LIKE 'family-pool:%' AND (g.id IS NULL OR t.type <> 'adjustment' OR t.amount >= 0 OR t.bookingId IS NOT NULL OR NOT EXISTS (SELECT 1 FROM family_group_members m WHERE m.groupId = g.id AND m.userId = t.userId))) findings ORDER BY recordId LIMIT 50;
-- inactive_family_retains_miles (error; owner: family-pool)
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'inactive_family_retains_miles' AS finding, COUNT(*) AS total FROM (SELECT id AS recordId FROM family_groups WHERE status = 'inactive' AND pooledMiles <> 0) findings;
SELECT /*+ MAX_EXECUTION_TIME(5000) */ 'inactive_family_retains_miles' AS finding, recordId FROM (SELECT id AS recordId FROM family_groups WHERE status = 'inactive' AND pooledMiles <> 0) findings ORDER BY recordId LIMIT 50;
ROLLBACK;
